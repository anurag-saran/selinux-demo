# Local-connection lab (Ansible runs *on* the SELinux host).
# Two RHEL boxes from a laptop: use inventory.dev.yml / inventory.production.yml
# (scripts/setup_rhel_hosts.sh). Demo app is shopapi (demo_bootstrap.sh --shopapi-only).

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_become: true
  vars:
    app_name: shopapi
    domain: shopapi_t
    install_root: /opt/shopapi
    var_dir: /var/lib/shopapi
    log_dir: /var/log/shopapi
    runtime_dir: /run/shopapi
    service_name: shopapi.service
    selinux_ops_from_package: false
    selinux_ops_dir: "{{ playbook_dir }}/../scripts"
    policy_artifact_dir: "{{ playbook_dir }}/.."
    policy_pp_src: "{{ policy_artifact_dir }}/selinux/shopapi/shopapi.pp"
    app_manifest_path: >-
      {{
        (policy_artifact_dir | dirname) ~ '/config/' ~ app_name ~ '.manifest.yml'
        if (policy_artifact_dir | basename) == 'policy_out'
        else policy_artifact_dir ~ '/config/' ~ app_name ~ '.manifest.yml'
      }}
    ansible_env_path: "/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
